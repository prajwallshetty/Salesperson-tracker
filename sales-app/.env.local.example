## Base URL of the Express/Socket.IO backend (server/). Safe to expose to the browser
## (no secrets) — the only env var the field app needs. Secrets stay in server/.env only.
NEXT_PUBLIC_API_URL=http://localhost:4000
