{
  "name": "salesforce-management-system",
  "private": true,
  "workspaces": [
    "server",
    "admin-web",
    "sales-app"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace server",
    "dev:admin": "npm run dev --workspace admin-web",
    "dev:sales": "npm run dev --workspace sales-app",
    "build:server": "npm run build --workspace server",
    "build:admin": "npm run build --workspace admin-web",
    "build:sales": "npm run build --workspace sales-app",
    "seed": "npm run seed --workspace server"
  }
}
